import SettingsPage from '@/components/settings/SettingsPage';

export const metadata = {
  title: 'Settings | GameArchade',
  description: 'Manage account and app settings',
};

export default function SettingsRoutePage() {
  return <SettingsPage />;
}
