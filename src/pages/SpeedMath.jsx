import React from 'react';
import SpeedMathContainer from '../components/speedmath/SpeedMathContainer';

/**
 * Speed Math page component - handles routing and page-level concerns
 * The actual game logic and UI is handled by SpeedMathContainer
 */
const SpeedMath = () => {
  return <SpeedMathContainer />;
};

export default SpeedMath;
