import React from 'react';
import SlidingPuzzleContainer from '../components/slidingpuzzle/SlidingPuzzleContainer';

/**
 * Sliding Puzzle page component - handles routing and page-level concerns
 * The actual game logic and UI is handled by SlidingPuzzleContainer
 */
const SlidingPuzzle = () => {
  return <SlidingPuzzleContainer />;
};

export default SlidingPuzzle;
