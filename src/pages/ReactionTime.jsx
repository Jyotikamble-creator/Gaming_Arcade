import React from 'react';
import ReactionTimeContainer from '../components/reactiontime/ReactionTimeContainer';

/**
 * Reaction Time page component - handles routing and page-level concerns
 * The actual game logic and UI is handled by ReactionTimeContainer
 */
const ReactionTime = () => {
  return <ReactionTimeContainer />;
};

export default ReactionTime;
