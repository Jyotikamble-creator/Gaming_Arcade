import React from 'react';
import QuizContainer from '../components/quiz/QuizContainer';

/**
 * Quiz page component - handles routing and page-level concerns
 * The actual quiz logic and UI is handled by QuizContainer
 */
const Quiz = () => {
  return <QuizContainer />;
};

export default Quiz;
